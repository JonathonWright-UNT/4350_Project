In order for this program to work, you will need to install a few items.
Use the commands below to install these neccary packets:
(You may need to use python, python3, pip, or pip3)

python3 -m pip install PyMySQL

pip install flask flask-wtf flask-sqlalchemy wtforms[email]

pip install flask-bcrypt

pip install flask_login

sudo pip install flask_mail
